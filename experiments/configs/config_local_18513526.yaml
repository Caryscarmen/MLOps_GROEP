experiment_name: "mlp_baseline"
seed: 42
device: "cuda"

data:
  data_path: "/scratch-local/scur2395.18513526/data"
  batch_size: 128
  num_workers: 4

model:
  name: "mlp"
  input_shape: [3, 96, 96]
  num_classes: 1
  hidden_units: 128

training:
  epochs: 5
  learning_rate: 0.001
  save_dir: "experiments/results"
