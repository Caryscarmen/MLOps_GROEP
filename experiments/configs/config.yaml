experiment_name: "mlp_baseline"
seed: 42
device: "cuda"

data:
  data_path: "/scratch-shared/scur2395/surfdrive"
  batch_size: 128
  num_workers: 4

model:
  name: "mlp"
  input_shape: [3, 96, 96]
  num_classes: 1
  hidden_units: 128

training:
  epochs: 3
  learning_rate: 0.001
  log_interval: 100
  save_dir: "experiments/results_seed42"
